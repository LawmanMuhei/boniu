namespace MiniView.WebView2App
{
    // Single source for application metadata and bootstrap extraction directory.
    internal static class AppVersion
    {
        internal const string Current = "1.8.0";
        internal const string Assembly = Current + ".0";
    }
}
