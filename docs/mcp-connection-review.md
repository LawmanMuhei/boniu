# 新 MCP 连接与工作区复查

日期：2026-09-22。

## 连接与使用规则

- 已连接当前 ShunCode 工作区，根目录为 `D:/Cline/douyin-mini-webview2`。
- 文件发现优先使用 `list_directory` / `find_files`，内容定位使用 `search_files`，符号导航使用 `lsp`。
- 修改前使用 `read_files` 获取当前内容与版本；工作区修改统一使用 `apply_patch`，随后检查 `get_diagnostics` 并运行相关构建或测试。
- 多步骤工作使用 `set_todos` 保存完整任务状态；临时进度使用 `report_progress`。
- 长命令通过 `run_command` 启动，并用 `get_command_output`、`send_command_input`、`cancel_command` 管理。
- 工作区外的本机路径只通过 `run_command` 先做只读定位，不把工作区工具的空结果误判为文件不存在。

## 当前工作区快照

- 当前分支为 `main`，HEAD 为 `7ec7d15`。
- 相对 HEAD 有 13 个已跟踪文件被修改、25 个未跟踪文件；这些变更尚未提交。
- 已跟踪差异统计为 406 行新增、2487 行删除；主要原因是将约 2900 行的 `MainForm.cs` 按职责拆分为多个 partial 文件。
- 新增了版本单一来源、版本一致性检查、统一测试脚本、更新链路离线测试、页面与媒体脚本模块、可见性感知 WebView、窗口生命周期代次及多份实施/复审文档。
- 昨日实施记录声称 x64/x86 自测、冒烟与回归测试均通过，但本次连接复查没有重新构建或运行测试。
- `git diff --check` 未发现补丁格式错误；仅显示现有 LF 到 CRLF 的工作区换行提示。
- `dist/` 仍是修改前发布文件；当前源码改动尚未提交、推送或发布。

## 后续工作基线

接下来的任务应以当前未提交工作区为准，不能用 `7ec7d15` 的旧单文件结构覆盖这些变更。修改业务代码前先读取对应拆分文件，并在交付前至少运行目标架构的 `scripts/test.ps1`；涉及发布时再执行版本检查、双架构测试、候选构建与人工验收。
