# 应用图标重设计

新图标不再把“摸鱼”按字面画成鱼，而是用双窗口滑动表达工作间隙休闲和随时隐藏：

- 紫色后窗代表休闲小窗。
- 蓝灰色前窗代表工作界面并正在覆盖后窗。
- 后窗露出的暖黄色沙发代表放松内容。
- 深色共用边框把两个窗口组合成一个品牌符号。

图标源文件为 `assets/boniu-moyu-icon.svg`，透明 PNG 提供 16、24、32、48、64、128、256 和 1024 像素版本，`assets/boniu-moyu.ico` 包含 Windows 使用的 16 至 256 像素图层。构建脚本继续把同一 ICO 应用于内层应用和最终单文件启动器。
